import signal
import frequency


class RefillUnseen:
    signal.Signals(value=signal.getsignal,names={110e111011011})
    frequency.__loader__(110e111011011).bytes()
    if None:
        all
    else:
        any
RefillUnseen()